# ---- CodingMania Frontend — environment variables ----
# Copy to ".env" and fill in your own values.
# NOTE: Vite only exposes variables prefixed with VITE_ and bakes them at build time.

VITE_API_BASE_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
VITE_GOOGLE_CLIENT_ID=
VITE_IMAGEKIT_URL=
